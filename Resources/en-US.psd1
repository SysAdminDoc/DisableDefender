@{
    Culture = 'en-US'
    Direction = 'LeftToRight'
    Strings = @{
        'app.name' = 'DisableDefender'
        'app.tagline' = 'Microsoft Defender disabler / remover (firewall preserved)'
        'menu.disable' = '  [1] Disable  (reversible; policy + tasks + passive mode + services)'
        'menu.remove' = '  [2] Remove   (aggressive; Safe Mode recommended; SecHealthUI + SafeBoot trap)'
        'menu.restore' = '  [3] Restore  (replay and verify the recorded baseline)'
        'menu.status' = '  [4] Status   (show current Defender + firewall state)'
        'menu.health' = '  [5] Health   (compare current state to expected target)'
        'menu.offline' = '  [6] Prepare Offline (generate WinRE/offline remove script bundle)'
        'menu.quit' = '  [Q] Quit'
        'menu.select' = 'Select'
        'menu.invalid' = "  Invalid selection: '{0}'"
        'cli.aborted' = 'Aborted.'
        'cli.health.target' = 'Health target: {0}'
        'cli.health.summary' = 'OK={0} Drift={1} Unknown={2} Total={3}'
        'cli.health.reapply' = 'Reapply plan:'
        'cli.health.reapply.item' = ' - {0}'
        'cli.offline.generated' = 'Offline remove bundle generated:'
        'cli.offline.script' = '  Script: {0}'
        'cli.offline.version' = '  Version: {0}'
        'cli.offline.usage' = 'Usage from WinRE or secondary Windows install:'
        'cli.operation.verified' = 'Verified operation: attempted={0} changed={1} verified={2}'
        'cli.reboot.prompt' = 'Reboot now? (Y/n)'
        'cli.reboot.warning' = 'Rebooting in 15 seconds...'
        'cli.error.title' = 'DisableDefender error'
        'gui.recovery.hub' = 'Recovery hub'
        'gui.recovery.title' = 'Verify the selected target before resuming, rolling back, or exporting evidence.'
        'gui.recovery.subtitle' = 'All queries and exports stay local. A cancellation request stops at a safe query boundary.'
        'gui.recovery.close' = 'Close recovery hub'
        'gui.recovery.refresh' = 'Refresh evidence'
        'gui.recovery.cancel' = 'Cancel query'
        'gui.recovery.done' = 'Done'
        'gui.recovery.live' = 'LIVE TARGET EVIDENCE'
        'gui.recovery.drift' = 'Exact drift (first 12)'
        'gui.recovery.phase' = 'PHASE RECOVERY'
        'gui.recovery.snapshots' = 'SNAPSHOTS & DIFF'
        'gui.recovery.snapshots.description' = 'Choose a baseline, save a target-aware snapshot, or compare against live state.'
        'gui.recovery.exports' = 'LOCAL EXPORTS'
        'gui.recovery.exports.description' = 'Exports use the selected target and never upload data.'
        'gui.recovery.sense' = 'Sense is not included unless the selected command explicitly opts in.'
        'gui.recovery.snapshot' = 'Save snapshot'
        'gui.recovery.compare' = 'Compare to live'
        'gui.recovery.report' = 'HTML report'
        'gui.recovery.support' = 'Support bundle'
        'gui.recovery.target' = 'Health target'
        'gui.recovery.ready' = 'Ready'
        'gui.security.open' = 'Open Windows Security'
        'gui.log.copy' = 'Copy'
        'gui.log.export' = 'Export'
        'gui.log.clear' = 'Clear'
        'gui.operation.cancel' = 'Cancel operation'
        'gui.action.disable' = 'Disable Defender'
        'gui.action.remove' = 'Full Remove'
        'gui.action.restore' = 'Restore Defender'
        'gui.action.repair' = 'Repair defaults'
        'gui.action.refresh' = 'Refresh status'
    }
}
